# HowLowCGet

The program should allow the user to:
- enter a decimal number and see its 8-bit binary form
- enter an 8-bit binary number and see its decimal value
- add two 8-bit binary numbers
- show the carry bit
- show whether overflow occurred
- find the two’s complement of a binary number
- subtract one binary number from another
- interpret the same bits as unsigned and signed